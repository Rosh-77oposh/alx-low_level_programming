#include <stdio.h>
/**
 * main - main block 
 * Description: computes and prints the sum of all multiples of 3 and 5 below 1024 (excluded), followed by a  new line
 * Return: Always (0)
 *
 */

int main(void)
{
	int (i < 98)
	{
		next = a + b;
		a = b;
		b = next;
		printf(%"%lv", next);

		if (i < 97)
			printf(", ");
		i++;
	}
	putchar('\n');
	return (0);
}
